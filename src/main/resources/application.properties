# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://shinkansen.proxy.rlwy.net:10501/railway}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:fdOHLAPnTVSiBFYGcIdIhrZikVvDcoOV}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS Configuration
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# Jackson Configuration
spring.jackson.time-zone=Asia/Colombo

# Admin Registration - Allowed Email Domains
admin.allowed.email-domains=${ADMIN_ALLOWED_EMAIL_DOMAINS:@gmail.com,@iit.ac.lk}

# JWT Configuration (Admin only)
jwt.secret=${JWT_SECRET:0000120012002120013303232303232303232303232320323230323230323223232032}
jwt.expiration=${JWT_EXPIRATION:21600000}

# Logging
logging.level.com.metropolitan.backend=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Application
spring.application.name=metropolitan-backend

# Groq AI Configuration
groq.api.key=${GROQ_API_KEY:}
groq.api.model=openai/gpt-oss-120b
groq.api.base-url=https://api.groq.com/openai/v1
